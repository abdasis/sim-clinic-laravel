<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true">
    <!--
        Konfigurasi test terhadap PostgreSQL — skema yang dipakai produksi.

        Migration foreign key RESTRICT sengaja dilewati di SQLite karena
        SQLite tidak mendukung drop foreign key, sehingga ForeignKeyRestrictTest
        hanya benar-benar berjalan lewat konfigurasi ini.

        Default mengikuti docker-compose.yml (port 5435, user/pass postgres),
        dengan database terpisah agar data dev tidak ikut terhapus RefreshDatabase.

        Siapkan sekali:
            docker compose up -d db
            docker compose exec db createdb -U postgres sim_clinic_laravel_test

        Jalankan: php artisan test -c phpunit.pgsql.xml

        Nilai di bawah tidak dipaksa (tanpa force), jadi environment variable
        dari shell tetap menang bila setup Anda berbeda:
            DB_PORT=5432 DB_USERNAME=lain php artisan test -c phpunit.pgsql.xml
    -->
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="CACHE_STORE" value="array"/>
        <env name="DB_CONNECTION" value="pgsql"/>
        <env name="DB_HOST" value="127.0.0.1"/>
        <env name="DB_PORT" value="5435"/>
        <env name="DB_DATABASE" value="sim_clinic_laravel_test"/>
        <env name="DB_USERNAME" value="postgres"/>
        <env name="DB_PASSWORD" value="postgres"/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
    </php>
</phpunit>
